const developmentNotification = (title, body) => {
  console.log(
`
################################################
# Title: ${title}
# Body: ${body}
################################################
`);
};

export default developmentNotification;
